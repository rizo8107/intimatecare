// This is a placeholder file to prevent build errors
// The actual component has been removed from the project

const TelegramLoginButton = () => {
  return null;
};

export default TelegramLoginButton; 